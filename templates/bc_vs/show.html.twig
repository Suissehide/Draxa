{% extends 'base.html.twig' %}

{% block title %}BCVs{% endblock %}

{% block body %}
    <h1>BCVs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ b_c_v.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ b_c_v.date ? b_c_v.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Accompagnant</th>
                <td>{{ b_c_v.accompagnant }}</td>
            </tr>
            <tr>
                <th>Permission</th>
                <td>{{ b_c_v.permission }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ b_c_v.etat }}</td>
            </tr>
            <tr>
                <th>MotifRefus</th>
                <td>{{ b_c_v.motifRefus }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('b_c_vs_index') }}">back to list</a>

    <a href="{{ path('b_c_vs_edit', {'id': b_c_v.id}) }}">edit</a>

    {{ include('b_c_vs/_delete_form.html.twig') }}
{% endblock %}
